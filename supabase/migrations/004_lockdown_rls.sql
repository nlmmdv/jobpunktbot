-- 004_lockdown_rls.sql — ОТМЕНЕНО / NO-OP (2026-07-15)
--
-- ⚠️ ВНИМАНИЕ. Исходное содержимое этой миграции (включение RLS + удаление всех
-- политик по всей схеме public) ВЫЗВАЛО ИНЦИДЕНТ: оно сняло политики общей
-- платформы ПВЗ, которая живёт в ЭТОЙ ЖЕ базе Supabase. Эффект отменён вручную
-- восстановлением политик из бэкапа (backup 15 Jul 00:29 → temp project → re-apply).
--
-- Эта БД — ОБЩАЯ с большой платформой, её схема и RLS ведутся ВНЕ этого репозитория.
-- НЕ применять здесь массовых изменений RLS и НЕ запускать `supabase db push`/`db pull`
-- против этого прод-проекта. Файл намеренно оставлен пустым (no-op), чтобы прежний
-- деструктив не повторился.

SELECT 1;  -- no-op
